## Packages
framer-motion | Smooth animations for tabs, cards, and transitions
recharts | Data visualization for goal history and progress
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes
date-fns | Date formatting for logs and history
lucide-react | Beautiful icons for the UI (already in base, but ensuring availability)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["'DM Sans'", "sans-serif"],
  display: ["'Outfit'", "sans-serif"],
}
